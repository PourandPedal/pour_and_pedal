= @event.title

= form_tag charges_path do
  %article
    %label.amount
      %span
        = number_to_currency(@event.price)
  %script.stripe-button{"data-amount" => (@event.price * 100), "data-description" => @event.title, "data-key" => Rails.configuration.stripe[:publishable_key], :src => "https://checkout.stripe.com/v2/checkout.js"}